import React from 'react';
import { BrowserRouter as Router, Switch, Route } from 'react-router-dom';

const Routes = () => (
  <Router>
    <Switch>
      {/* <Route component={ReceipesList} /> */}
    </Switch>
  </Router>
);

export default Routes;
